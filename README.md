# i-am-losing-my-mind

This is an open-source experimental repo. You can inspect it, fork it, clone it, and run it locally.

## Local setup

Install dependencies:

```bash
npm install
```

Run the app locally:

```bash
npm run dev
```

Build for production:

```bash
npm run build
```
